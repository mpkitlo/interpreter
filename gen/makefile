
build_dir = build
exe = interpreter

$(exe):
	mkdir -p $(build_dir)
	ghc -Wall -Wno-unused-imports --make -outputdir $(build_dir) -o $@ -isrc/ -isrc/generated Main.hs

clean:
	rm -rf $(build_dir) $(exe)

.PHONY: clean